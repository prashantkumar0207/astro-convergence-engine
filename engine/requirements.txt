# Canonical runtime dependencies (audit F-16: the previous file
# pinned "pysweph", a wrong package name, and omitted jsonschema).
pyswisseph==2.10.3.2
tzdata==2025.2
jsonschema>=4
# Optional API surface:
# fastapi + uvicorn are only needed to serve engine/main.py.
