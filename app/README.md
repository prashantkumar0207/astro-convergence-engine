# app/

End-user facing application layer(s). Intentionally empty at skeleton stage - no product
scope is ratified (docs/OPEN_QUESTIONS.md Q5, Q8).

Standing rules:
- Depends on `engine/` and on `knowledge/` contracts only; never on `research/`.
- UI/product decisions require roadmap phase entry criteria to be met.
